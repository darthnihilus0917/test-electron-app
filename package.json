{
  "name": "rnd-electron-app",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --platform=win32 --arch=x64 --out=deploy --overwrite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@electron-forge/cli": "^7.4.0",
    "electron": "^30.0.3"
  },
  "dependencies": {
    "bootstrap": "^5.3.3",
    "fs": "^0.0.1-security",
    "jquery": "^3.7.1"
  }
}
